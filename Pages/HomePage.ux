<Page ux:Class="HomePage">
    <JavaScript File="HomePage.js" />
    <ScrollView>
        <StackPanel>
          <Each Items="{sleepLogs}">
            <Button Text="{dag}" Clicked="{chooseLog}"/>
          </Each>
        </StackPanel>
    </ScrollView>
</Page>
